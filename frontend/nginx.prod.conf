# ================================================
# 🌐 MoreTech.VTB Frontend - Nginx Production Config
# ================================================
# Оптимизированная конфигурация для продакшн
# с проксированием API, WebSocket и безопасностью
# ================================================

# Основной HTTP блок
server {
    listen 80;
    server_name localhost;
    
    # Корневая директория приложения
    root /usr/share/nginx/html;
    index index.html index.htm;

    # ================================================
    # 📦 Настройки сжатия для оптимизации
    # ================================================
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_comp_level 6;
    gzip_types
        text/plain
        text/css
        text/xml
        text/javascript
        application/javascript
        application/json
        application/xml
        application/xml+rss
        application/atom+xml
        image/svg+xml;

    # ================================================
    # 🛡️ Заголовки безопасности
    # ================================================
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "no-referrer-when-downgrade" always;
    add_header Content-Security-Policy "default-src 'self' http: https: data: blob: 'unsafe-inline' 'unsafe-eval'" always;

    # ================================================
    # 📂 Статические файлы с кэшированием
    # ================================================
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot|map)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        add_header Access-Control-Allow-Origin "*";
        try_files $uri =404;
    }

    # Специальная обработка для иконок и изображений
    location ~* \.(png|jpg|jpeg|gif|ico|svg)$ {
        expires 6M;
        add_header Cache-Control "public, immutable";
        add_header Access-Control-Allow-Origin "*";
        try_files $uri =404;
    }

    # ================================================
    # 🔄 API Проксирование к Backend
    # ================================================
    location /api/ {
        # Убираем /api из пути при проксировании
        rewrite ^/api/(.*) /$1 break;
        
        proxy_pass http://backend:5000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Таймауты (увеличены для загрузки видео)
        proxy_connect_timeout 300s;
        proxy_send_timeout 300s;
        proxy_read_timeout 300s;
        
        # Настройки для больших файлов (видео)
        proxy_request_buffering off;
        proxy_buffering off;
        
        # CORS заголовки
        add_header Access-Control-Allow-Origin "*" always;
        add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS" always;
        add_header Access-Control-Allow-Headers "Authorization, Content-Type" always;
        
        # Обработка OPTIONS для CORS
        if ($request_method = 'OPTIONS') {
            add_header Access-Control-Allow-Origin "*";
            add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS";
            add_header Access-Control-Allow-Headers "Authorization, Content-Type";
            add_header Access-Control-Max-Age 1728000;
            add_header Content-Type "text/plain; charset=utf-8";
            add_header Content-Length 0;
            return 204;
        }
        
        proxy_cache_bypass $http_upgrade;
    }

    # ================================================
    # 🎤 WebSocket для голосовой транскрипции
    # ================================================
    location /ws/voice {
        proxy_pass http://transcription:8001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "Upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # WebSocket специфичные настройки
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 86400s;
        proxy_send_timeout 86400s;
    }

    # ================================================
    # 🗣️ WebSocket для TTS (синтез речи)
    # ================================================
    location /ws/speak {
        proxy_pass http://tts:8003;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "Upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # WebSocket специфичные настройки
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 86400s;
        proxy_send_timeout 86400s;
    }

    # ================================================
    # 🤖 AI-HR Service проксирование
    # ================================================
    location /ai-hr/ {
        rewrite ^/ai-hr/(.*) /$1 break;
        
        proxy_pass http://ai-hr:8002;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Таймауты для AI обработки
        proxy_connect_timeout 120s;
        proxy_send_timeout 120s;
        proxy_read_timeout 120s;
    }

    # ================================================
    # 🏠 React Router (SPA) - обработка всех маршрутов
    # ================================================
    location / {
        try_files $uri $uri/ /index.html;
        
        # Кэширование HTML файлов
        location ~* \.html$ {
            expires 5m;
            add_header Cache-Control "public, must-revalidate";
        }
    }

    # ================================================
    # 🏥 Health Check
    # ================================================
    location /health {
        access_log off;
        return 200 "Frontend is healthy\n";
        add_header Content-Type text/plain;
    }

    # ================================================
    # 📊 Метрики для мониторинга
    # ================================================
    location /metrics {
        access_log off;
        return 200 "# MoreTech.VTB Frontend Metrics\nfrontend_status 1\n";
        add_header Content-Type text/plain;
    }

    # ================================================
    # 📝 Логирование
    # ================================================
    access_log /var/log/nginx/access.log combined;
    error_log /var/log/nginx/error.log warn;

    # Отключаем логирование для статических ресурсов
    location ~* \.(css|js|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        access_log off;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}

# ================================================
# 🔧 Дополнительные настройки Nginx
# ================================================

# Размер буферов для больших запросов (увеличено для видео)
client_max_body_size 500M;
client_body_buffer_size 128k;
client_header_buffer_size 1k;
large_client_header_buffers 4 16k;

# Настройки keep-alive
keepalive_timeout 65;
keepalive_requests 100;
